export default defineNuxtPlugin((nuxtApp) => {
    // Initialize Bootstrap tooltips, popovers, etc. if needed
    nuxtApp.provide('bootstrap', {
      // Expose Bootstrap methods if needed
    })
  })